console.log(5 + 5);

const X = 5, Y = 5;
console.log (X + Y);

//after updating this code it dosen't work because we declare the variable using constant key work "const" it dosen't allow us to change.
//because of that it shows error.
 X = 6;
 Y = 6;
 console.log (X + Y);

