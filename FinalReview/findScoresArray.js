/* eslint-disable */

function findAverage(num) {
  let sum = 0;
  for (let i = 0; i < num.length; i++) {
    sum = sum + num[i];
  }
  return suu/num.length
}
let x = [10, 20, 40, 50];
console.log(findAverage(x));
